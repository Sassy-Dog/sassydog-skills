# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repo is

A **Claude Code plugin marketplace** containing a single plugin (`sassy-dog`) that bundles reusable skills and review agents for Claude Code, Gemini CLI, and other AI coding tools. Repo visibility is **`PUBLIC`** — a deliberate exception to the org's `INTERNAL` default, taken on 2026-08-12 because the claude.ai account-level plugin marketplace **cannot sync a non-public repo** (a non-public source fails with `Marketplace sync failed` regardless of org app install or SSO state; see [#175](https://github.com/Sassy-Dog/sassydog-skills/issues/175)). Two consequences follow and must not be undone by a later "align with the org default" sweep: org secrets with `private` visibility do not reach this repo (that is *desirable* here — see [#178](https://github.com/Sassy-Dog/sassydog-skills/issues/178)), and CI runs on GitHub-hosted runners rather than the self-hosted fleet, because `on: pull_request` fires for fork PRs. Licensed Apache-2.0.

There is **no build step** — the entire repo is Markdown (skills/agents) plus the Bash scripts bundled inside skills' `scripts/` directories. CI (`.github/workflows/ci.yml`, required on `main`) runs `bash scripts/preflight.sh` — the single source of truth for every gate: shellcheck (`-S warning`), `scripts/check-frontmatter.sh` (two layers — **loader** requirements on every tracked skill/agent file: frontmatter `---` on line 1, `name`/`description` present, name matches directory/filename; plus the **Agent Skills spec** constraints on `SKILL.md` only: `description` ≤ 1024 characters, `name` ≤ 64 characters with charset and hyphen rules, and a strict frontmatter key allowlist whose unknown keys hard-fail, so CI cannot go green on a file `gh skill publish` would refuse — `agents/*.md` are exempt from the spec layer because an agent legitimately carries `color:`), the positional-token and legacy-name guards, manifest JSON validation + the CalVer version-of-record guard, the versioning tests (`scripts/test-versioning.sh`), the ownership-matcher tests (`scripts/test-ownership-matchers.sh` — the `setup-hooks`/`setup-deps` `generated-by:` matchers, extracted from the shipped `SKILL.md` and run against real pre-rename consumer artifacts committed verbatim under `scripts/fixtures/legacy-markers/`), the label-taxonomy tests (`scripts/test-label-taxonomy.sh` — the cross-set CIEDE2000 colour check via `align-labels.sh --collisions`, `issue-claim.sh`'s label reconcile exercised against a mock `gh`, and the no-third-copy guard that searches every colour from both `taxonomy` emitters across the tracked tree and fails on any hit outside its own home; definitions and a mock only, never a live repo), the label-migrate tests (`scripts/test-label-migrate.sh` — `align-labels.sh --migrate`'s relabel-then-delete gate against a mock `gh` that records writes, plus the source-level single-call-site invariant and a mutation that neuters the gate; the mode itself is **never** run in CI, because it deletes labels in other repos), the dependabot-render tests (`scripts/test-dependabot-render.sh` — `setup-deps` renders one lane per (ecosystem, **directory**) and every lane is backed by a manifest in the directory it names, against three recorded consumer layouts under `scripts/fixtures/dependabot-render/`, with the collapsed-to-`"/"` pre-#169 shape required to FAIL validation; recorded file lists only, no repo and no network), the detect-hook-stack tests (`scripts/test-detect-hook-stack.sh` — `setup-hooks`' `has_tracked` probe against a **20k-path** index-only fixture, because the bug it pins is size-dependent and a small fixture passes either way: under `pipefail` a `git ls-files … | head -1 | grep -q .` returns the writer's SIGPIPE **141** once the match list overruns the ~64KB pipe buffer, so a match read as a miss and the render silently dropped that tool's route (issue #172). The gate asserts the pre-fix shape still fails on that fixture — a fixture that stopped tripping it would make the test vacuous — plus a source-level guard that `has_tracked` carries no pipeline at all), the visibility precondition tests (`scripts/test-visibility-preconditions.sh` — `setup-deps`' App-token renders have **two** preconditions, not one: a merge gate AND a non-public repo, because its org secrets are `private`-visibility and so resolve to empty strings in a public repo, failing weeks later on that repo's next Dependabot PR as an auth error that looks unrelated (issue #178). **The precondition belongs to the credential, not to one workflow**: three templates mint `PLATFORM_WRITER_APP_*` from the same two secrets, #178 gated only `dependabot-auto-merge`, and the two it missed — both lockfile-sync templates — are precisely the two that `actions/checkout` the PR head ref inside a job minting that credential (issue #186, hence the rename from `test-auto-merge-visibility.sh`; #232 later bounded the token's on-disk lifetime to the push step in both — `persist-credentials: false`, no `token:` on `checkout` — but the render-time veto is what keeps the credential out of a public repo at all). The decision is `SKILL.md` prose rather than script, so the gate pins the instruction — the same source-level shape as the label-migrate single-call-site invariant — and is mutation-proved against the pre-fix wording. **Its must-not-exist assertions run against a whitespace-flattened copy of each file**, because this repo hard-wraps prose and a line-scoped grep for forbidden wording turns a line wrap into a false PASS — the failure mode that matters, since a must-exist check merely fails loudly under the same wrap; two tracked files, no `gh`, no network), the verify-issue-refs tests (`scripts/test-verify-issue-refs.sh` — the grooming-drift checker's near-match pool must hold the language the repo is actually **written in**. Its symbol harvest is keyword-led (`fn|def|function|class|…`) and a POSIX shell function definition carries no keyword at all, so `name() { … }` matched nothing: measured on `Sassy-Dog/platform`, whose primary language is bash, 368 names in the pool against 257 distinct POSIX-form definition names across 330 definition lines in tracked `*.sh`, **not one of them harvested** (issue #263; the issue's table counts 253 — same tree, same day, counting distinct names of *column-0* definitions only, which is why every figure here states its unit). An empty pool the header already reasoned about; a pool full of the *wrong* languages is the worse variant, because it does not degrade to silence — it fails in both directions at once. `platform#777` had a correct `read_monitors()` reported `DRIFT` suggesting `readMonitorsBody`, an unrelated TypeScript function that scores 0.759 against it, while a genuinely renamed shell function has no near match in that pool either and tiers `likely-new`, which `groom-backlog` §6 documents as **not a defect** — so real drift in a shell codebase passed the gate silently. **The fix is a SCOPED second pattern, and the scope is four rules, not one**, which is the part a later "why is this so complicated" sweep will thin out. (1) Harvest only from shell FILES (the same `name()` line is a JS/TS method shorthand and a K&R C definition). (2) Skip HEREDOC BODIES inside them — generators write `.ts` and `.c` payloads into `<<EOF` blocks in shell scripts, and a TypeScript method in one *resolved clean*, a false resolve and #263's silent half one door over; its cost on a healthy tree is now zero, so it earns its place in the invent direction rather than by what it removes. (3) Hand awk **absolute paths of regular non-symlink files** rather than git pathspecs (`--` stops option parsing but not pathspec magic, so one tracked `:(weird)name.sh` made `git grep -- <list>` exit 128 and the pool empty; a tracked `zero.sh -> /dev/zero` satisfies `[[ -r ]]` and awk reading it never returns, stacking one hung process per `/loop 5m` tick; a symlink *out* of the checkout puts another codebase's identifiers into a line this org pastes into a PUBLIC issue). (4) Require a BODY — `{` or `(` on the same line — because without it a bare `flush()` **call site inside a quoted `awk '…'` program** reads as a definition, which put a fabricated name into the shell half of the pool on the very repo #263 was measured on; measured across 823 real definitions in three repos, that rule drops exactly that one name and no real definition. **The heredoc tracker's own bugs are the fixtures**, because every one was live, and they split into two classes that are not equally survivable: a here-string (`<<<"$line"`) or a comment mentioning `<<EOF` read as an opener merely *skips* — it cost `align-labels.sh` 9 of its 14 definitions and `verify-issue-refs.sh` both of its own — while an indented terminator closing a plain `<<EOF`, a `<<-` stripping spaces (POSIX strips **tabs** only), a second opener never tracked (`cat <<A <<B`), and a legal digit-led delimiter (`<<2EOF`) and a backslash-quoted one (`<<\EOF`, POSIX's third quoting form) each end suppression early or miss it altogether and **invent** a name out of a payload, which is not survivable — so the delimiter keeps every character it has except those three quoting forms. The mirror of that is a PHANTOM heredoc — a `<<` that opens nothing, from a bit-shift (`$(( 1 << 2 ))`) or a mangled hyphenated delimiter — which skips every definition *below* it in that file rather than one name, and is what the digit-led relaxation cost until an all-digit delimiter was refused. **The union of the two pools carries NO third cap**: each half is bounded at 5000, and capping the merge as well is an alphabetical truncation of a set that just grew — measured on velovate-app it evicted 95 names the pre-#263 pool held and dropped 88 of 189 new shell names, i.e. #263's loud half reintroduced on the largest repos. The cap existed for argv, so the pool now travels by **temp file** — and the issue BODY with it, which is the half that matters: GitHub caps a body at 65,536 *characters* while Linux caps one argv element at 131,072 *bytes*, so ~44k CJK codepoints sits inside the first and clears the second. Measured, `main` exits **126** on such a body — not one of the documented 0/3/10/64, and read by both consumers as "not 3", i.e. promote and dispatch. **What it deliberately does NOT fix** is that the pool is one flat set, so a suggestion can come from a language the reference is not: measured, HEAD already answers `render_panel()` with a **Rust** `render_page` and exits 3, so shell names now behave the way every other language's already did, and origin-aware scoring would rewrite the tiering contract `dispatch-ready` consumes — its own issue. **The load-bearing assertion is fixture adequacy**, measured by RUNNING the pre-#263 script rather than transcribing its regex a third time, and the same floor now covers the hostile-filename tree. Acceptance's *unchanged for a non-shell tree* is measured too — eight bodies through both editions, byte-for-byte — but only because that fixture now **contains the shape**, and its method is deliberately named nothing any other case probes: naming it `open_at` put that token in the tree, where `resolve_symbol`'s `\b` mention probe resolves it **on Linux**, and case 3's DRIFT silently stopped happening on the one platform CI runs. Ten mutants, each proved applied (one line changed) before its proofs run — the #262 `cmp -s` lesson, and it fired for real here. Coverage is **derived by measurement**, mutations applied to the shipped source one at a time with the failing assertions recorded, which is what found every rule nothing pinned. **Acceptance item 1 is deliberately NOT mutation-proved**: git's `-E` engine honours `\b` on Linux (2.54/musl, 2.47/glibc) and matches **nothing** with it on macOS (2.55) — the script header stated that measurement as universal and now records the split — so a *defined* name is also a *mentioned* one and its pre-fix verdict differs per platform; pool membership is proved instead by names that appear nowhere in the tree. That same split leaves `resolve_symbol`'s mention fallback, which still probes with `\b`, answering "not mentioned" for everything on macOS and truthfully on Linux: a separate defect, out of #263's scope because it changes which findings are reported, and **not** an inconsistency to tidy into the harvest's spelling. **Three rules no Linux run can observe are asserted at SOURCE level** for exactly that reason — the `\b` spelling itself (wrong, it is green on CI and empties the pool on every Mac), the bash 3.2 empty-array guard, and the fact that bash 3.2 cannot parse a `#` comment inside a process substitution (an apostrophe there is read as syntax) — the comment detector and the boundary check each carrying their own positive controls — the boundary check is proved against `\\b`, GNU's `\\<` and no boundary at all, because an earlier version of it accepted two of the three. One tracked file read, three trees built under `mktemp -d`; no `gh`, no network), the scanning-states tests (`scripts/test-scanning-states.sh` — the code- and secret-scanning pulls' silent failure modes across seven sections: the four-state disambiguation (a 404 means BOTH "Advanced Security off" and "never analyzed"; collapsing them reports a never-scanned repo as clean), pagination past page 1 plus a `capped` mode that reports the page-cap floor as a measurement rather than a guess (probed live: `velovate`, true count 102), the ref filter (a PR-ref alert is not default-branch debt), the new-vs-inherited split (a rule straddling the boundary counts only its fresh alerts), autofix probe scoping (probed only for critical/high rules; medium stays unprobed), secret-scanning classification by `validity` (`active` isolated with no age floor, `unknown_validity` and `inactive` handled separately, `push_protection_bypassed` carried through), and a source-level guard — run against raw `SKILL.md` lines, not the flattened copy the prose gates use — that the `validity: "active"` P0 table row is unique (catches a contradictory duplicate a presence-only regex would miss) and carries no age-like token; mock `gh` only, no network), the sentry-verification tests (`scripts/test-sentry-verification.sh` — `setup-config` may only write a `sentry:` block for a project **verified by culprit** against the repo being configured, because a Sentry project and a repo can share a name and belong to different codebases, and every consequence of getting it wrong is invisible: the wrong repo claims another codebase's P0s, two repos double-report the same issues, and **both** plates still render complete. A failed verification records `sentry: none` — pinned here as the config contract's **first documented exception** to presence-is-the-toggle, which is exactly the prose a later "align with the governing principle" sweep deletes as drift — and the sibling prior-claim scan is pinned SECONDARY, since a cloud session has no sibling checkouts and would lose the guard silently (issue #213). **The `none` form now spans FOUR keys and is deliberately ASYMMETRIC across them** (issue #261): `sentry: none` keeps its blind-spot row, while `testflight: none`, `posthog: none` and `mobile: none` take one `(n/a)` token on `survey-work`'s `✓ Clean today:` line and no row at all. `sentry: none` is still the *first* documented exception, so that phrase stands — but the scope sentence beside it no longer does, and the asymmetry is what will be "aligned". It breaks in either direction and both are silent: collapsing `sentry: none` onto the clean line restores #213's gap, and promoting the other three back into rows restores #261, where `Sassy-Dog/platform` — Terraform, no app, no mobile target, no beta channel — carried three permanently unclearable rows on **every** plate, two of them in the loudest position §6 has, which is how a reader learns to skim the heading a genuinely dark Sentry lives under. So the rationale is pinned beside the table (*absent error monitoring is a gap somebody could close; an absent mobile app is a product fact*), along with the `board:` exclusion — §3B's boardless form means an absent `board:` selects a documented alternative path rather than going dark — and the no-blind-spot-row exclusions, without which the next reader generalises `none` to every key. The four-key table is checked by **counting** its rows rather than looking the four names up, since a lookup passes just as happily on a table that grew a fifth key or lost one, and "how many keys behave which way" *is* the decision. Two failure modes were measured rather than assumed: the per-key plate rules are read from §3 alone, because a whole-file window lands on §6's prose and reported a reverted probe rule as **covered**; and the three `none` values are asked in `interview.md` §2c and never defaulted, a guessed `none` being the same silent retirement of a real blind spot, run backwards. **Six of its own assertions have now been measured reporting a clean tree on a source stating the inverse, and every one was a scoping error** — the per-key windows read the whole flattened file and landed on §6's prose instead of the §3 probe rule they claimed to pin; `update-mode.md`'s rule was checked file-wide for a phrase step 1's unrelated rename-collision rule already carries, so inverting it to "always silently rewrite a `none`" left the gate printing `ok`; §2c's per-key check was a bare `grep -q "$key"` that its own rationale sentence satisfies; and §3A's `sentry:` rule was covered by *nothing*, because the per-key loop runs over the three clean-line keys only — so moving `sentry: none` onto the clean line left CI green, the headline decision unpinned in the one section the loop calls authoritative. `sentry` now has its own block with the expectations INVERTED, and the loop must not be widened to swallow it. **The same shape then recurred twice in §6, which is the section an agent actually follows when rendering** ([#268](https://github.com/Sassy-Dog/sassydog-skills/issues/268)): §6's Confirmed-N/A rule stated BOTH outcomes inside ONE window, so rewriting `sentry: none` to "aligned with the other three and takes the clean line too" left the gate GREEN — deleting sentry's affirmation made the three-key row veto *more* likely to pass, not less — and the `### Blind spots` rules were checked by CO-OCCURRENCE (`sentry: none` anywhere in the subsection AND `produces a row` anywhere in it), so swapping which key gets the row satisfied both halves. Both are now cut at the key literal, with sentry's half carrying the inverted expectations, and neither half may be folded back into the loop. The same review also found the carry-forward split (`sentry: none` is **not** in the `none` carve-out) pinned by a must-exist half alone at two of its four sites, so `setup-config/SKILL.md` could state that decision *and its inverse* and stay green — every site now carries the veto too; and `interview.md` §2c's adopt-mode "always" was prose no instruction implemented, since `update-mode.md`'s Adopt mode routes through create mode's *conditional* §2c trigger — adopt now has its own always-ask step, mirroring migrate's Step 2b. Two smaller traps are recorded in the script header because neither is guessable: the example-block must-not-exist check reads a **flattened** copy (MD013 is off here, so a hard-wrapped row is invisible to a line-scoped grep — measured both ways), and a backtick inside a double-quoted `ok`/`bad` message is **command substitution**, which three messages in that file were silently performing. **Every review round so far has found assertions here passing on sources stating the inverse, and the reason the author's own mutation set kept reporting "all detected" is the lesson worth carrying to every other prose gate in this list: every mutation had been written by deleting the exact literal its assertion greps for, which is tautologically caught and proves only that `grep` works.** Re-measured with mutations that invert the *meaning* and word it as a tidying editor would — different verb, different emphasis, different sentence shape. The family is always the same: **negation-blindness, whose first two fixes were each worse than the bug in the other direction** (`grep -qi 'blind-spot row'` matches inside "**no** blind-spot row"; vetoing the word `no` *anywhere in the window* was tried three times and defeated each time, because the negator merely shared the window rather than governing the mention; the six-verb affirmative that replaced it enumerated an **open** class, so `plus a blind-spot row in §6` and `also produces` evaded it while the natural rewords `keeps a blind-spot row` and `gets its own blind-spot row` reddened the gate on prose that was *right* — six correct edits turned `main` red, measured. Destinations are now matched verb-independently and each mention is classified against its own bounded left context, against a **closed** function-word negator class; `none` is deliberately not in it, since `sentry: none` sits immediately before its own destination in the one rule the gate exists for. The classifier is a **polarity** rule, not a word list: a negator inside a prepositional phrase negates that phrase (`a repo **with no** Sentry keeps a blind-spot row` is an affirmation) and a second negator cancels the first (`is **not exempt from** a blind-spot row` likewise), because lengthening the list is precisely what the six-verb version already failed at. **All three of the classifier's scans stop at the same clause boundary, through one shared `clause_break` helper** ([#270](https://github.com/Sassy-Dog/sassydog-skills/issues/270)): two of them did and the cancellation scan did not, an asymmetry that reads as deliberate to the next person and was not — `that is not so. sentry is exempt from a blind-spot row` let a *previous* clause's negator cancel a real negation, so the sentence read AFFIRMED and could satisfy the very must-affirm veto that exists to catch it, which is #268's direction one more time. The helper takes the **raw** token as well as the bare one, because `bare()` strips the trailing terminator and a scan holding only bare words cannot see a boundary at all — passing the bare word twice reproduces the bug while looking like the fix, and a mutation pins that. All three stay **count**-bounded — four content words in the two look-around scans, four *positions* in the cancellation scan, a real difference and not a wording slip: bounding by clause instead is [#271](https://github.com/Sassy-Dog/sassydog-skills/issues/271), which subsumes this, and removing a count bound here is the line between the two. Two **known limits are measured and recorded in the file**, one of them the fix's own cost. The three dash arms are **dead in all three scans and in every form** — they are *equality* tests, so an attached dash (`special-`) never satisfies one either, and a standalone dash bares to the empty string, which every scan skips before testing; only `[;:.]$` has the attached-to-a-word behaviour, and neutering all three dash arms changes no assertion. Reviving them moves where the two already-bounded scans stop, on prose the gate classifies correctly today, so it is left to #271. The cost is that the `:` arm is now **live inside the cancellation scan**, where a config-key token (`sentry:`) is likelier than a real colon clause and where — unlike the left-context scan — there is no parenthetical skip: measured, `testflight: none is not, like sentry: none, exempt from a blind-spot row` goes 1/0 to 0/1, the quiet direction, with the colon-free aside unchanged to isolate the arm. It is accepted rather than special-cased because both repairs are worse — dropping `:` from the shared set weakens the two scans that were already right, in exactly #270's own direction, and giving the cancellation scan a set of its own re-creates the divergence the fix exists to remove. No prose in the tree hits it, and a parenthetical skip there belongs with #271. One helper also makes the boundary SET a single edit, which is the risk the extraction itself adds: measured, adding `,` to it — the one addition a tidying editor would reach for, and the one the header rules out by name — rewrote all three scans and left the **entire** gate green, so the classifier now carries a battery of its own — fixed strings owing nothing to what any tracked file says today, every case of it proved load-bearing by a mutation, and no count of them transcribed here, for the same reason this gate carries no inventory number at all); **silence mistaken for a negation** (a `'0'` veto tests only "nothing affirmed", so a rule whose negative clause drifted out of its window scored 0/0 and the veto printed `ok` while measuring nothing — rules that *state* a negative now use a `'-'` want that requires the negation to be present); **a marker mistaken for a destination** (`\(n/a\)` sat inside the clean-line vocabulary, so "they appear under Blind spots with an explicit `(n/a)` marker" satisfied a clean-line assertion — separate constants now); **window sizing** wrong in both directions (460 bytes read half of a ~930-byte rule so a must-not-exist slipped past; 400 bytes let one key's assertions be satisfied by the next key's rule ~158 bytes away; and a cap that binds *before* its stop marker is the same defect wearing the other face — `carry_win`'s last asserted phrase ended at byte 933 of a 937-byte window, so `window_is_bounded` now asserts every such window ends where it claims to); **a window right for one check being wrong for another** (the `none` clause is the right scope for "does it keep its row" and the wrong scope for "does this rule ever mention the clean line", since a contradictory clause appended to the rule's absent-key half lands outside it — both scopes are now asserted); **tallying instead of classifying** (counting two patterns left a fifth key matching neither invisible, and the row filter keyed on a backtick so a hand-added row without code formatting was invisible too — every row must now be *accounted for*); **prefix anchors** (`cannot re-derive is the` is satisfied by "…is the **surface**", the exact inversion of "…the **confirmation**"); **a mention mistaken for a commitment** (`grep -qi 'migrate mode'` was satisfied by a sentence *demoting* migrate mode); and **emphasis** (``The exception is scoped to **`sentry:`** alone.`` slipped past the plain flatten, so must-not-exist checks now also run against an emphasis-stripped copy). Source-level like the visibility gate, must-not-exist checks flattened for the same reason. **It carries no inventory number, deliberately**: this sentence used to read "39 semantic mutations, 0 stale and 0 undetected, 59 assertions measured load-bearing", every figure of which was refuted by the next person who re-measured — and a number nobody can re-derive from the tree is exactly what a future editor trusts *instead* of re-measuring (#268). The run prints its own assertion count and enforces a coarse vacuity floor beneath it; the mutation batteries live in the PRs that added them. Eight tracked files, no `gh`, no network), the sentry-counts tests (`scripts/test-sentry-counts.sh` — `sentry-triage` may only gate on counts it has **confirmed as lifetime** (issue #218). `count`/`userCount` mean different things per transport under identical labels and neither transport says which it returned: REST is lifetime, the MCP's `search_issues` rescales to its `period` argument. Measured on velovate, `VELOVATE-WORKERS-2` returned 1 event / 1 user against a true 30 / 3 — and the 30d pull also returned 1 event, so widening the window is not the fix. Re-verifying the fix live surfaced a second rescaled field #218 never recorded: `firstSeen` came back as *2 days ago* for a fingerprint first seen 57 days earlier, which points triage at the latest deploy for a long-running fault — pinned here too, along with the distinction that the `firstSeen:` **query term** was never shown to be broken and must not be conflated with the returned field. **It survived because the edition under daily human review was the accidentally correct one**: the REST-based `daily-fire-watch` ranked it a P0 the same morning the MCP-based interactive run put the whole Sentry surface on the clean line, so the wrong answer never looked wrong. The gate pins the discovery-vs-scoring split, the confirm step's position *before* the gate step, the ban on choosing what to confirm by windowed count — which reads exactly like a missing optimization and re-creates the bug one step earlier — and both halves of `skip-unconfirmed`, which never qualifies and is always reported. Source-level and flattened like the two gates above, mutation-proved against all five pre-fix files; no `gh`, no network, no Sentry call), the security-listing tests (`scripts/test-security-listing.sh` — a **security-labelled issue is never collapsed into a bare count** (issue #219). `whats-on-fire`'s label map reached security only through (`bug` **and** `security`), so an issue's visibility turned on whether it happened to be phrased as a defect — and most security work is not one: hardening, a missing control, an unmodelled sanitizer, a policy decision. Two velovate security issues filed the same day at the same `sev:medium` split P1/P2 on that single label, and the P2 one — 72 open CodeQL alerts including 10 sites logging raw rider coordinates on a product live with real users' location history — was absorbed into a bare integer in every `daily-fire-watch` post since it was filed. **The fix is a LISTING rule, not a promotion**, and that is the part that will drift: #219 proposed promoting `security` to P1 outright, so a reader arrives already holding the rejected option, and promoting would re-derive a priority the same file forbids re-deriving two paragraphs earlier. The gate pins the tier-untouched wording, the `unranked` case, and the survival of the re-derive principle itself; source-level and flattened, mutation-proved against both pre-fix files), the doc-reconciliation tests (`scripts/test-doc-reconciliation.sh` — **all three shipping paths must tell the agent to reconcile docs against the repo before the PR body is drafted** (issue #220). Docs are an input to no other gate, so lint, type, test and the review agent all pass on a PR whose `CLAUDE.md` now states the opposite of what the repo does. It gates the **instruction**, never staleness itself — #220 rules that out, since staleness is a semantic judgement with nothing to grep for, and a script pretending otherwise reproduces the skimmed-past output #199 documents. `take-it` and `dispatch-ready` are the half that matters: they dispatch sub-agents that open PRs from a cold worktree and never see an interactive session's instructions, so a rule living only in `send-it` never runs for them. Pins both traps in **both directions** — a closed issue does not prove the behaviour landed, and an OPEN one does not prove it did not — plus the scope limiter, because an unbounded doc step is one nobody runs. Its flatten strips blockquote markers, since `take-it`'s rule sits inside a `>` prompt template where a wrapped phrase otherwise reads as absent; three tracked files, no `gh`, no network), the poll-queue-eject tests (`scripts/test-poll-queue-eject.sh` — `pr-shepherd`'s queue poller may only call a PR **`ejected` on evidence it actually holds** (issue #234). The #60 race guard anticipates the PR-*state* flip lagging the queue-entry removal and disambiguates on the last `RemovedFromMergeQueueEvent`; the removal *event* lags too, and an empty timeline does not distinguish "never enqueued" from "not visible yet". A tick landing in that sub-second window reported `qr-ninja#847` — merged cleanly the same second — as `EJECTED`, wrong in both directions at once: a loud terminal verdict whose attached guidance points a human at re-enqueueing something already landed, and a silent `"result":"ejected"` in the final JSON that a coordinator branches on with no warning. The disproof was already in the script and already being printed in the failing line — `QSTATES`, the last queue-entry state seen. The lag case is now non-terminal and bounded by the **global** `POLL_MAX_TICKS`/exit 124 ceiling *alone*: a per-PR counter was explicitly rejected, because its fall-through verdict re-creates the same false eject on a merely slow timeline, so the gate asserts the unresolved run reaches that ceiling rather than resolving early. It pins the two cases that must stay terminal and immediate — a genuine eject, and a PR that truly never enqueued (the `--auto` method-flag trap) — since a fix that swallowed those would trade a loud wrong answer for a silent one. Prose checks are flattened, both call sites being hard-wrapped; mutation-proved against the pre-fix script. Mock `gh` serving recorded GraphQL payloads: no repo, no network), the template-actionlint tests (`scripts/test-template-actionlint.sh` — the three `setup-deps` **workflow templates** are linted by `actionlint`, in a **render** (issue #245). Bare `actionlint` lints `.github/workflows/*`, which in this repo is `ci.yml` and nothing else, so the highest-consequence YAML here — `pull_request_target`, a minted `PLATFORM_WRITER_APP_*` token, a push to a PR head ref, the same surface #178/#186/#232 kept finding defects in — was checked by nothing, and a defect there never reddens this repo: it ships to every consumer, where Dependabot answers a broken workflow by silently doing nothing. **The templates cannot be linted where they live** — their render-time `# {{IF:FLAG}}` blocks and `{{TOKEN}}` substitutions parse as errors that are not defects — so the gate renders each one in every documented shape (both auto-merge arms; each lockfile template nested and at the repo root) and lints the renders, automating the by-hand step #232's agent performed. Since shellcheck is on PATH, actionlint's shellcheck integration covers the templates' `run:` blocks too. The custom `sassy-dog` self-hosted label — the one expected finding — is **declared** through an actionlint config file rather than muted with `-ignore`, so every other unknown label still fails, and a mutation proves that. Four vacuous-green guards, because each way of covering nothing reads as a pass: a tracked template no variant renders, an `{{IF:}}` arm that is OFF in every variant, a leftover token in a render, and a missing `actionlint` — a courtesy skip locally (the coverage and render assertions still run), a hard failure under `CI=true`. It is the one gate `preflight.sh` **cannot** run in CI: `ci.yml` puts the pinned binary on `$GITHUB_PATH`, which reaches only later steps, and preflight runs before that install — so CI owns it as its own step and preflight skips it there, the same split as the bare actionlint gate. Renders into a tmpdir: no repo, no network), the review-orchestrator allowlist tests (`scripts/test-review-orchestrator-allowlist.sh` — every agent `pr-review-orchestrator` can dispatch, and every reviewer `config-contract.md` names as a legal `review_surfaces:` value, must **ship in this plugin** (issue #246). Nothing pinned that: the nine dispatch targets were checked by hand when #236 landed and by nothing since. The failure is invisible to the author who ships it — `react-typescript-engineer` and `iac-cloud-architect` exist as **user-level** agents in a typical Sassy Dog developer's `~/.claude/agents/`, so a PR adding one to the surface table passes CI, **works when the author tests it**, and fails only in a consumer repo where that agent does not exist; #236's own proposal named both, and only a by-hand look at `agents/` caught it. Hence a file-listing comparison against `agents/`, the posture #167 established. Its scope is the **map, not one document**: the orchestrator's surface table, every `*-reviewer` token in its body, the `### review_surfaces` section of `config-contract.md` — whose transcription of the nine is the third-copy shape itself — and every `review_surfaces:` block in any other tracked doc (`README.md` today). Map values are read after the LAST colon, so the **bare** unprefixed name, which is how #236's sketch actually wrote it, is caught alongside the namespaced one; a `sassy-dog:<name>` that is not a reviewer must still resolve to a shipped agent *or skill*, so prose may name `sassy-dog:send-it`. Mutation-proved on all four extraction paths against a synthetic `agents/` mirror whose completeness is asserted first — as an EQUALITY with the real verdict rather than as 'clean', so a genuine regression fails in one place instead of also blaming the harness — plus two vacuity floors at today's nine, since a broken extractor reports a clean tree exactly like a correct one. Two tracked files and a tracked-Markdown sweep; no `gh`, no network), the gotcha-claims tests (`scripts/test-gotcha-claims.sh` — a `gotcha_summary` claim about issue state may only reach an issue body once it has been **confirmed against that issue** (issue #249). The field is free prose in a *frontmatter* slot, so it inherits neither protection the config format provides — nothing derives it, and it is outside the `##` lane a human curates — making it the one field that can assert a time-varying fact and never be revisited; `groom-backlog` then copies it verbatim to a cold worktree agent with no way to check it. `solador`'s config asserted "#15 is not finished — #308 (updater) and #334 (Windows + Authenticode) remain" for nine days after all three closed, while the *same repo's* same claim in a `##` prose section was corrected in two — the seam is the format's, not one operator's. **The load-bearing fixture is the KNOWN-STALE one**: a verifier that accepts everything passes every clean config, so only a config the mock `gh` flatly contradicts distinguishes one that works. The second failure mode is **degradation** — a missing `gh`, an undetermined repo, a failed lookup — where the tempting shape is a skip and a skip is a silent pass on exactly the input the gate exists for, so unresolvable drops like a contradiction does and the source carries no skip exit at all. Its no-gh PATH is a symlink sandbox rather than `/usr/bin:/bin`, which on a GitHub-hosted runner would quietly mean "`gh` present, network reachable". **The third failure mode is upstream of the classifier entirely** (issue #262), and it is the one that does more than lose a claim: fail-closed classification is only as good as the unit handed to it, and the sentence splitter could not see backticks, so the `;` in `` `code=0; cmd || code=$?` `` ended a claim mid-span. The tail kept the `(#N)` and dropped as unverifiable — correctly, and invisibly. The truncated **head** cited no issue at all, was therefore classified an *invariant*, and was emitted inside the `--- BEGIN SAFE GOTCHAS ---` markers that `groom-backlog` §4 orders copied verbatim into issue bodies: the tool **fabricated a truncated instruction and certified it as verified**, into the one artifact a cold worktree agent is told to treat as ground truth, and the rule it mangled was the errexit exit-code idiom. Shell idioms are exactly where `;`, `.`, `!` and `?` live (`. ./scripts/lib.sh`, `[ ! -f "$f" ]`, `[ $? -ne 0 ]`), so the field's most useful entries were the likeliest to be cut. So inline code is **parsed** before the split — spans pair by backtick RUN, since a ``` ``…`` ``` delimiter otherwise binds its two opening ticks to each other and reproduces #262 in full — and an unpaired run quarantines the **whole** field, because greedy pairing can mis-bind *before* the stray tick. **But the guard that bounds the family is GROUP LINKAGE, and the history of the ones it replaced is the reason to leave it alone.** Three narrower fixes each decided WHERE the cut lands, and each had a next input: backtick *parity* (blind to a cut between an even number of ticks, and to one in text with no ticks); a padded-span *heuristic* (caught three reported shapes, missed a fourth, and dropped legal input — `` `` ` git add .; git commit ` `` `` is correctly paired Markdown, and padding is *required* when span content starts or ends with a backtick). Which ticks the author meant is not decidable from the text, so the splitter stops guessing: fragments of a sentence are tagged with a group, resolved individually, and **committed together** — if any fragment drops, none of that group is certified. A mis-parse then costs a drop, never a half-sentence presented as verified. Both deleted guesses are recorded in the script header so they are not reintroduced. **The root case has no backticks in it at all**: `;` matched the sentence-boundary regex, so a clause was an independent claim, and `Set FOO=1; the guard is disabled per #999, which remains open. Always export it.` dropped the middle clause and emitted `Set FOO=1; Always export it.` — grammatical, confident, inverted, and present on `main`. `;` joins clauses inside a sentence and no longer ends one; `e.g.`/`i.e.`/`etc.` are handled by the same linkage. **Its fixture is deliberately not drawn from this repo's own config**, which returns `claims=5 kept=5 dropped=0` before *and* after — not because that field has no in-span dots (it has, in `` `.claude-plugin/plugin.json` ``) but because no in-span `.` is followed by a space, which is what the boundary regex matches. **Deciding where a sentence STARTS turned out to be the same guess as deciding where to cut, and it was wrong in both directions** — `Deploy only to U.S. East until #999 …` certified `Deploy only to U.S.`, a complete and meaning-inverted sentence, while a lowercase continuation welded two real ones. So the default is INVERTED: a new group needs positive evidence (the previous fragment ends in a terminator *and* the token before it is not abbreviation-shaped — a single letter, anything internally dotted, or four characters or fewer). **The accepted cost is over-linking, and it is not a bug to fix back**: `Pin every action to a full SHA. bun install must run …` loses the SHA invariant, because `SHA.` is abbreviation-shaped. A lost invariant is visible in the report; a fabricated one is not. **The class is bounded, not closed** — a sentence whose referent was dropped can still survive, and resolving that means anaphora. Every input that defeated a previous fix is kept as a fixture, and **the mutation scoping is itself asserted** rather than described: a coverage matrix measures, for every (fixture, rule) pair, whether removing that rule reproduces that fixture's specific fabrication, and compares it to a declared table. Its detector is deliberately GENERAL — a mutant fabricates when it certifies text the unmutated run does not — because the first version probed for a per-fixture literal and was **wrong in four cells**, one of which declared the unpaired-run quarantine redundant while removing it certified a truncated fragment at exit 0. A literal derived from today's behaviour cannot see a fabrication that truncates differently tomorrow. Measured: four of the five rules are each **load-bearing** for at least one fixture (asserted as a vacuity floor) though only three are ever the *sole* protection, one fixture has two, and only the `;` rule has no fixture — which is **not** grounds for deleting it, since its job is that a citation is judged on a whole sentence rather than a clause of it. Without it, a narrow mutation whose fixture some other rule started covering would quietly prove nothing and still report success. Its `apply_mutation` refuses the vacuity `cmp -s` allows — **`cmp -s` exits 2 on a MISSING file, which an `if` reads as "they differ"**, so a sed that matched nothing reports `mutation applied` and every negative assertion after it passes against a file that was never run. Mock `gh` only: no repo, no network), the review-gate decision tests (`scripts/test-review-gate-decisions.sh` — the **five decisions settled about the review gate**, all of them prose and every one of them reading like drift to a future "align with the governing principle" sweep: three from #237 about `send-it`'s gate (issue #247), and two more from #248 about the two *dispatching* paths (issue #255). **The gate is unconditional**: an absent `review_agent:` resolves the shipped `sassy-dog:pr-review-orchestrator`, so no `send-it` run can silently ship unreviewed — and #235's verbatim `review: SKIPPED — no review_agent resolved (lint/type/test only)` line **survives that default** as the backstop for *resolution* failure (the agent does not exist, the plugin did not load, the dispatch errored). To a reader who sees only "there is always a default" that line is dead code, and deleting it restores the original silent-no-review bug wearing a default, because a run that printed nothing is indistinguishable from one that reviewed cleanly. **`review_agent` is deliberately NOT presence-is-the-toggle** — it has a default, so #243 moved it *out* of that key list and moved `review_surfaces` (no default) *in*; in the key list that reads as a plain inconsistency, and "restoring" it turns the absent key back into an off switch for every consumer repo at once. **The opt-out is `review_agent: skip`, not `none`** — `none` belongs to the confirmed-absent exception pinned by `test-sentry-verification.sh`, which records an absence somebody went and *checked* for, and that justification does not apply to a key that merely overrides a default; the missing symmetry is exactly what a tidying sweep closes, and #261 growing that form from one key to four makes the pull stronger, not weaker (the contract now names `review_agent:`/`review_site:` as excluded, and why). **`review_site` is CONFIGURED and not derived, deliberately** — repo visibility *is* derivable (`gh repo view --json visibility`), so a literal reading of *configure only what cannot be derived* deletes the key outright, and the paragraph explaining why it is an exception is the first thing a "simplify this" sweep drops. Deriving it live means a visibility change silently rewrites a repo's review architecture: taking a repo private downgrades it from pre-PR review to after-the-fact review with no config diff, no prompt and no line in any run's output — the [#187](https://github.com/Sassy-Dog/sassydog-skills/issues/187) failure class. `setup-config` seeds it **once** from visibility and records the resolved value, and the half that survives a *refresh* lives in a different file: Phase 4 of `setup-config/SKILL.md` carves `review_site` out of update mode's "re-verify every fact against live state" rule, pinned **scoped to that phase**, because a carve-out that drifts elsewhere leaves the rule reading absolute where it runs. **A Blocking finding blocks the merge, with exactly ONE redispatch** — surfaced named, one redispatch carrying the finding as context, then `blocked`; never merged past, and never parked back in Ready. The number is what rots, since "one" alone reads as an arbitrary retry count, so the rationale is pinned beside it: it is the *same* single-redispatch budget a failed check already gets. Source-level like the visibility, sentry-verification, sentry-counts, security-listing and doc-reconciliation gates, with must-not-exist checks flattened for the same reason — proved rather than assumed here: the same forbidden wording hard-wrapped between `codegen` and `review_agent` is invisible to a line-scoped grep and caught by the flattened one. Its flatten also strips blockquote markers, like the doc-reconciliation gate's, because `take-it`'s fix-Blocking-findings-before-committing rule sits inside a `>` sub-agent prompt where a wrapped phrase otherwise reads as absent (measured both ways). It also uses **no `| grep -q` pipeline anywhere**, that being the #172 shape in which `grep -q`'s early close makes `pipefail` promote the writer's SIGPIPE 141 and report a caught mutation as a miss. Twenty-one mutations across all five decisions; six tracked files, no `gh`, no network), the pipefail-grep guard (`scripts/test-pipefail-grep.sh` — **no script under `pipefail` may feed an *unbounded* writer into `grep -q`** (issue #256, generalising [#172](https://github.com/Sassy-Dog/sassydog-skills/issues/172)). `grep -q` closes the pipe on its first match, the writer takes SIGPIPE, `pipefail` promotes the 141 — so the pipeline reports **failure precisely when it matched**, and only once the writer's output outruns the ~64KB pipe buffer, which is why the identical line is correct today and wrong later with no code change at all. Nothing generalised #172: the detect-hook-stack gate pins one function *by name*, and the shape recurred in PR #252, where a `scan … | grep -q` made **three of that gate's four mutation proofs report `undetected`** — a gate whose whole purpose is refusing vacuous greens was itself reporting vacuously, and it surfaced only because its author checked the proofs actually fired. **The rule is deliberately narrow and is the part that will be "simplified"**: flag every pipeline into `grep -q` whose *source* stage is not `printf` or `echo`, purely syntactically, with no judgement in the script — which found twelve sites, every one of them fixed in the same PR, and proved on a 20k-path index that `setup-config`'s `git ls-files | grep -q` capability probes were returning **141 for a match**, silently reporting Prisma, EF-Core, `build_runner`, .NET and iOS as absent in exactly the large consumer repos they exist for. Both wider options were rejected on #256 and must not be re-derived: a per-site opt-out marker (~128 annotations, and a marker that dense becomes something pasted unread) and a blanket ban (a ~131-line rewrite of the very scripts this repo relies on to catch its own regressions). Its **known limitation is stated in its own header** — a very large variable through `printf` slips past, because no syntactic check can know a variable's size; the shape that has actually bitten twice is a *command* writer. `shellcheck` does **not** cover this, verified against 0.11.0 rather than assumed: it says nothing at all about the shape, and its SC2143 actively *recommends* `grep -q`. Exemptions are a central table inside the guard keyed by (file, distinctive substring) — never an inline marker, which is the rejected design — and a stale one **fails**; there is exactly one, the pre-#172 shape `test-detect-hook-stack.sh` transcribes verbatim as its fixture-adequacy probe. Fixtures live under `scripts/fixtures/pipefail-grep/` and are deliberately not `*.sh`: the guard is inside its own scan scope, so a `printf`-built fixture puts the risky shape on a live line and the guard flags itself — measured, not predicted. Six mutations, no `gh`, no network), and markdownlint (`.markdownlint-cli2.jsonc`) — plus two separate actionlint steps running a pinned native binary (not the dockerized action): the bare tree-wide run, and the rendered-template gate above. **Run `bash scripts/preflight.sh` locally before every PR** (`--fix` auto-fixes markdownlint findings). Those gates are necessary but not sufficient: "correctness" still means accurate trigger phrases and skill instructions that actually work when invoked — verify changes by installing the plugin locally (below) and exercising the skill.

## Layout (flat — everything at root)

This repo *is* the plugin; there is no nesting under a plugin subdirectory.

```
.claude-plugin/
  plugin.json        # plugin manifest — version is the version-of-record, stamped by scripts/stamp-version.sh (never hand-edit)
  marketplace.json   # marketplace entry pointing at the GitHub repo
agents/
  *-reviewer.md      # subagents, auto-discovered, namespaced sassy-dog:<name>
  pr-review-orchestrator.md  # diff-scoped fan-out over the nine reviewers
skills/
  <skill>/
    SKILL.md         # required: frontmatter (name + description) + instructions
    references/      # optional: deep reference docs (progressive disclosure)
    scripts/         # optional: executable tools the skill calls
```

## How the pieces fit together

Two non-obvious architectures live in this repo.

### Generator + capability skills (the dev-workflow family)

- **The six workflow skills are generic and ship in the plugin.** `survey-work` (prioritized work plate), `groom-backlog` (backlog grooming), `take-it` (parallel issue-shipping, "take #341, #432"), `dispatch-ready` (loop dispatcher), `send-it` (single-PR end-to-end), and `tidy-repo` (post-shipping git reconciliation) each have exactly ONE implementation, under `skills/`. Per-repo behavior lives in that repo's `.claude/sassy-dog/<skill>.md` config, read at load time via dynamic context injection. The format is `setup-config/references/config-contract.md`. **There is no per-repo copy of these skills** — that is what makes every invocation render namespaced (`sassy-dog:survey-work`) and what removes the trigger-phrase competition an earlier generated-skills design had to design around.
- **`NO_CONFIG` is a first-class state.** A skill invoked in a repo with no config must derive what it safely can and run in its most conservative mode. Two exceptions block instead, because both act unattended and outward-facing: `take-it` (dispatches sub-agents) and `dispatch-ready` (dispatches and merges on a loop).
- **Configure only what cannot be derived.** Repo slug, default branch, and `delete_branch_on_merge` come from `gh repo view` at runtime; they never appear in config. A configured value is a value that can drift.
- **Workflow skills are thin and delegate.** Shared mechanics live in the capability skills — `github-issues` (board/issue reads, dedupe-then-file writes), `sentry-triage` (gate-and-escalate), `pr-shepherd` (polling, merge queue vs direct, coupled-PR serialization, worktree teardown), `repo-cleanup` (the `tidy-repo` engine: `[gone]`/squash branch sweep, stash triage, untracked sweep — reuses `pr-shepherd`'s `teardown.sh --sweep`), `repo-health` (TODO/CI/lag scans, and the three security-exposure pulls: Dependabot, code scanning, secret scanning), `testflight`. They invoke them by namespaced name (`sassy-dog:<skill>`); fix mechanics in the capability skill, never in a workflow skill. **`repo-cleanup` keeps its name** — it is now a capability-boundary distinction (mechanics vs. the user-facing flow) rather than the trigger-collision avoidance it was originally named for.
- **Consumer repos must declare BOTH the marketplace and the plugin in their own `.claude/settings.json`.** Two keys, and both are load-bearing in cloud sessions: `extraKnownMarketplaces` declares where the `sassydog-skills` marketplace lives (`{"sassydog-skills": {"source": {"source": "github", "repo": "Sassy-Dog/sassydog-skills"}}}`), and `enabledPlugins` selects the plugin from it (`"sassy-dog@sassydog-skills": true`). `enabledPlugins` alone is **not** sufficient: it references the marketplace only by name, and marketplace registration otherwise lives in user-level state (`~/.claude/plugins/known_marketplaces.json`), which never transfers to a cloud VM — so every local session works (the user registered the marketplace once) while a cloud session or scheduled routine cannot resolve `@sassydog-skills` and silently loads no skill (issue #97: the daily fire-watch routine fired and produced nothing). Cloud sessions install repo-declared plugins at session start *from the marketplace the repo declares* (docs: Settings → `extraKnownMarketplaces`; Cloud environments → "What carries over"); this requires network access to the marketplace source. This repo declares itself; see `.claude/settings.json`.
- **Routine prompts carry a skill-load fallback as contract, and its degraded-load signal is a REQUIRED FIELD, never a discretionary sentence.** **A scheduled routine cannot load a plugin skill at all** — there is no supported mechanism, so the clause is not a hedge against an occasional failure, it is the only thing making these routines work ([#175](https://github.com/Sassy-Dog/sassydog-skills/issues/175): the routine-scoped `enabled_plugins`/`extra_marketplaces` fields accept API writes and persist nothing at both `create` and `update`, the routine UI exposes no plugins control, and an account-level plugin install reaches interactive claude.ai sessions only — verified by two runs, 20s and 5min after a confirmed install). Treat the clause as permanent contract: every scheduled-routine prompt that invokes a plugin skill must include it so the gap degrades loudly instead of silently no-op'ing. **A "note the degraded load" sentence alone is insufficient — it is discretionary, and it has already been dropped**: on 2026-08-11 the weekly run hit `Unknown skill: sassy-dog:whats-behind` in its own log and delivered a complete, authoritative-looking report with no note at all, while the run 24h earlier carrying the identical clause did include one (#146, and it poisoned #144's evidence). A missing sentence reads as "fine"; an empty slot in a structured header reads as broken. So both report skills carry an always-rendered `Load:` field on their header line (`plugin` or `fallback (degraded)`), and the routine clause names it. Exact clause for the daily fire-watch routine: "If `sassy-dog:whats-on-fire` is not among your available skills, read `skills/whats-on-fire/SKILL.md` from the repo checkout and follow it exactly, noting the degraded load in your output and rendering `Load: fallback (degraded)` in the report header." Exact clause for the weekly routine: "If `sassy-dog:whats-behind` is not among your available skills, read `skills/whats-behind/SKILL.md` from the repo checkout and follow it exactly, noting the degraded load in your output and rendering `Load: fallback (degraded)` in the report header." Use the same shape (skill name + its `skills/<name>/SKILL.md` path + the `Load:` field) for any new routine. **The field is still the run reporting on itself, so it is never the authority: where the report and the run log disagree, the run log wins.** The out-of-band check that settles it — the `Unknown skill: sassy-dog:<name>` signature, where it appears in a run log, and how to read its absence — is [`docs/ROUTINES.md`](docs/ROUTINES.md); consult the log, not the report, whenever load state is being used as evidence.
- **`setup-config` is a config generator, not a runtime skill.** Run inside a product repo it writes `.claude/sassy-dog/*.md` plus the `.claude/settings.json` plugin declaration. It renders no skill bodies. Its modes: **migrate** (a repo still on generated skills — extract config from them, verify, then delete), **update** (config already present), **adopt** (legacy hand-written skills), **create** (nothing yet). Siblings `setup-hooks` and `setup-deps` follow the same generator pattern for hooks and Dependabot config.
- **`setup-repo` is the umbrella orchestrator over those three, and carries no generation logic.** It owns only the broad "set up this repo" intent — the three generators keep their own specific vocabularies, and a description that swallowed them would make the specific invocations ambiguous. It picks which generators apply, prints one combined plan of every file they would touch, gets approval, then runs them **strictly in sequence** (`setup-config` → `setup-hooks` → `setup-deps`) and reports what ran, what was skipped, and why — a skipped generator is a reported outcome, never a silent no-op. **The order is load-bearing.** `setup-config` and `setup-hooks` both write `.claude/settings.json` — the marketplace/plugin declaration and the `hooks.PostToolUse` entry — and both merge surgically into their own keys rather than rewriting the file, so sequential runs compose while a concurrent or last-write-wins run drops one of the two *silently* (both contracts are report-and-skip, not error; a prior migration lost a consumer's `PreToolUse` push guard to exactly this class of overwrite). Never dispatch the generators as parallel sub-agents. Mechanics stay in the generators: fix detection, mode selection, or ownership matching there, never in the orchestrator.
- **One delegation mode.** Independent/vendored mode was removed — it was the only path that could produce an unbranded, un-namespaced skill, and no repo used it. Everything delegates `Skill: sassy-dog:<cap>`; `pr-shepherd` remains the script root (`repo-cleanup` ships no scripts and calls its `teardown.sh`; `github-issues` calls its `gh-retry.sh`).
- **`setup-hooks` is the same generator pattern one layer down**: it detects a consumer repo's stack (keyed on repo config presence, never installed binaries) and renders a PostToolUse formatter/linter dispatcher into `.claude/hooks/sassydog-post-edit.sh` plus a stack-agnostic stray-artifact guard into `.claude/hooks/sassydog-artifact-guard.sh`, and their `.claude/settings.json` entries. The guard is the one piece that is NOT stack-conditional — it is always rendered, it also owns a `Stop` entry (matcher-less; Stop carries no tool name), and it ensures `/tmp/` is gitignored, because a guard whose destination is not ignored only relocates the mess. It has **two ownership markers, and they behave differently under a rename**: the *settings entry* is owned by command path (`.claude/hooks/sassydog-`), which no producer rename touches; the *script* is owned by its `generated-by:` producer name, which is committed inside every consumer repo. So the script matcher must accept every producer name this generator has emitted (`setup-hooks`, `refresh-hooks`, `refresh-sassydog-hooks`) across both marker namespaces (`sassy-dog:` and `ai-agent-skills:`), emit the current name on write, and normalise — a matcher narrowed to the current name would classify every pre-rename consumer script as hand-written and skip it *silently*, since the contract is report-and-skip, not error. Re-runs reconcile only owned entries and never touch hand-written hooks. The template (`references/templates/sassydog-post-edit.template.sh`) is valid shellcheck-clean bash with ALL tool blocks present — rendering only deletes `# {{IF:TOOL}}` blocks, so every render is valid by construction. Like the config generator, it always runs from the plugin.
- **The two generators' render invariants are deliberately DIFFERENT, and neither should be restated as the other's.** `setup-hooks` stays **deletion-only** (above): one hook script per repo, no per-location repetition, so every render is valid by construction. `setup-deps` is **validated after render** (issue #169): a Dependabot lane is a (ecosystem, **directory**) pair and a monorepo needs the same ecosystem at several directories — velovate npm at four, tailoredtip bun at two — which deletion cannot express, so `dependabot.yml.template` v3 repeats a block per location and `scripts/validate-dependabot.sh` parses the render and asserts every emitted `directory:` actually holds the manifest it claims. That trade is strictly stronger *here*: the static guarantee never caught the bug, because a v2 render was always valid YAML that pointed at directories with no manifests, and Dependabot answers that by silently doing nothing. Two consequences worth keeping: `render-dependabot.sh` **refuses to default a lane to `/`** when a location cannot be derived (it drops the block and says so), and `validate-dependabot.sh --compare-to` catches the **diverged-but-owned** file — marker says "mine", content is past what a fresh render reproduces — and fails instead of overwriting, which is the state tailoredtip was in when this was found.
- **Config format: frontmatter is regenerated, `##` prose is carried across verbatim.** That split is the whole point — prose is what a refresh must never rewrite. Contract: `setup-config/references/config-contract.md`; migration: `references/migrate-mode.md`. **The split assumes frontmatter is *derived*, and several keys are not** — the exceptions are enumerated in `setup-config/SKILL.md`'s guardrail list, which is the copy to trust, and this sentence must not be read as a closed set. `gotcha_summary` is free prose in a frontmatter slot, filled once at setup and recomputed by nothing, so it is neither re-derived nor curated (issue #249). `review_site` is seeded once from visibility and then frozen (#248/#255, above). The three confirmed-absent answers `testflight: none` / `posthog: none` / `mobile: none` are carried forward for a third reason — they record a *check that happened*, and reading the tree can show a surface is present but never that somebody looked and concluded it is absent (issue #261); **`sentry: none` is deliberately NOT among them** and is re-derived every refresh, because it is also written when the culprit check merely *could not run*, so freezing it would let one unlucky session retire the plate's highest-signal surface with no path back. What is unique to `gotcha_summary` is not being non-derived but being **unbounded free prose**, which is why the enforcement below is about it alone. Its rule is therefore in the contract — **invariants only**, never an issue number with a state claim, an "as of `<date>`", or a roadmap position — and enforced where it is *used*: `groom-backlog` injects only what `github-issues`' `verify-gotcha-claims.sh` confirms against live issue state, and holds anything unresolvable rather than passing it to a cold agent. Moving the field into the `##` prose lane is the deeper fix and is deliberately separate work: it is a format migration reaching every consumer config, which this org rolls out by filing an issue per repo. **Marker recognition must accept every producer name** (`refresh-skills`, `refresh-sassydog-skills`, legacy `create-dev-workflows`) **and both marker namespaces** (the current `generated-by: sassy-dog:` prefix and the pre-rename `generated-by: ai-agent-skills:` prefix) — an unrecognised marker drops the repo into create mode and silently loses its config. That producer list is frozen history for the superseded `.claude/skills/<name>/SKILL.md` path; `setup-config` is deliberately **not** on it, because the `.claude/sassy-dog/*.md` config it writes carries no `generated-by:` marker at all.
- **Write paths are concentrated.** `github-issues/scripts/file-or-link-issue.sh` is the single issue-creation path (marker-keyed idempotency, `--dry-run`, preview-then-confirm, burst rail). `sentry-triage` never mutates Sentry. Keep it that way.

### Orchestrator + reviewer agents (`assess-it` and `pr-review-orchestrator`)

The nine `*-reviewer` agents have **two dispatchers and two modes**, and the modes are the whole design. Understanding either one means reading its orchestrator alongside the reviewers: `skills/assess-it/SKILL.md` + `orchestration.md` for audit mode, `agents/pr-review-orchestrator.md` for diff-scoped mode.

- **`assess-it` is the audit-mode orchestrator.** It runs a 5-phase audit: detect stack → fan out reviewer agents concurrently (one message, multiple Agent calls) → adversarially verify each finding → cluster into PR-sized work → preview and file GitHub Issues under a tracking Epic.
- **`pr-review-orchestrator` is the diff-scoped orchestrator** (`agents/pr-review-orchestrator.md`), dispatched by `send-it`'s review gate before the PR body is drafted. It is the **default** ([#237](https://github.com/Sassy-Dog/sassydog-skills/issues/237)): the gate resolves the repo's configured `review_agent:` if it has one and this orchestrator otherwise, so an absent key selects a review rather than switching one off, and **no `send-it` run** can silently ship unreviewed — the accepted cost being an extra review pass on every `send-it` run everywhere. Opting out is the explicit `review_agent: skip`. **That default is scoped to `send-it`; a second key carries the gate into the other two paths.** `take-it` and `dispatch-ready` dispatch sub-agents that open their own PRs from a cold worktree without invoking `send-it` (`skills/dispatch-ready/SKILL.md`: "a gate that lives only in `send-it` never runs for them at all"), and they carry most of the repo's PR volume — six PRs dispatched through `dispatch-ready` on 2026-08-24, none of them reviewed. [#248](https://github.com/Sassy-Dog/sassydog-skills/issues/248) closes that the way #220 closed the doc gate, by putting the instruction in all three shipping paths — but the right *site* differs per repo, so it is a config key rather than a copied paragraph. **`review_site: agent | coordinator`** (`skills/setup-config/references/config-contract.md`) chooses whether each dispatched sub-agent reviews its own diff **before** opening its PR or the dispatching loop reviews each PR **after** it opens; an absent key selects `agent`, the fail-safe site. It chooses only the site: `review_agent:`'s resolution order still chooses the agent, and nothing but `review_agent: skip` turns the gate off. **`setup-config` seeds it ONCE from `gh repo view --json visibility`** (PUBLIC → `agent`, INTERNAL/PRIVATE → `coordinator`) and writes the resolved value explicitly; update mode carries it forward. That last part is what a later "configure only what cannot be derived" sweep will try to delete, so the contract carries the justification beside the key: deriving visibility live means taking a repo private silently downgrades pre-PR review to after-the-fact review with nothing announcing it — the failure class [#187](https://github.com/Sassy-Dog/sassydog-skills/issues/187) documents. On the `coordinator` path a **Blocking** finding is never merged past: `dispatch-ready` §2 reuses its existing failure path — surface it named, one redispatch carrying the finding, then the `blocked` label — and never parks it back in Ready. **The default does not retire the `review: SKIPPED` line**, which still renders for the opt-out and, more importantly, whenever the resolved agent fails to dispatch — a resolution failure that printed nothing would be the original silent-no-review bug wearing a default. It reads the diff versus the *derived* default branch (never an assumed `main`), classifies changed paths into surfaces, fans out **only to touched surfaces**, runs its own integration-check pass for cross-surface concerns no single specialist sees, and returns one report split into **Blocking** vs **Nits**. **It dispatches only the nine reviewers this plugin ships** — the design it replaced named user-level agents (`react-typescript-engineer`, `iac-cloud-architect`), and a plugin default that fans out to an agent a consumer repo lacks fails the entire review rather than degrading (issue #236). Any new surface routes to one of the nine or to the orchestrator's own pass; adding a tenth dispatch target means shipping that agent here first. **`scripts/test-review-orchestrator-allowlist.sh` (gate 27) is what pins that**, and it covers the `review_surfaces:` examples below too — until [#246](https://github.com/Sassy-Dog/sassydog-skills/issues/246) nothing checked either, and the whole failure resolves on the author's own machine. **A repo can steer that classification without owning an agent** ([#238](https://github.com/Sassy-Dog/sassydog-skills/issues/238)): the optional `review_surfaces:` map in `.claude/sassy-dog/send-it.md` is a glob → reviewer map `send-it` forwards verbatim and Step 2 consults — the third tier, between the built-in heuristics and a bespoke `review_agent:`. Three constraints keep it from re-opening what #236 and #237 closed. Its **values are only those nine**, because #233's own sketch named `react-typescript-engineer` and `iac-cloud-architect` and a map naming a user-level agent steers on its author's machine and nowhere else. It is **additive** — it adds a reviewer to a path and has no form that removes one, so it cannot under-dispatch, which matters because an override-shaped `"**/*.md"` would send a repo whose skill bodies *are* its source back to being reviewed as prose. And an **unresolvable value discards the whole map and returns a Blocking finding**, reviewing the diff by the built-in table meanwhile: a typo costs the steering, never the review, and never a silently skipped surface. It steers Step 2 alone; steps 1 and 3–5 are untouched. `take-it` and `dispatch-ready` never forward one, so the review they now run under `review_site:` is an *unsteered* one — forwarding the map to them is a separate change, not something the key already does.
- **The `*-reviewer` agents are the fan-out workers for both.** Each owns a domain (architecture, code-quality, security, testing, cicd-release, infra-platform, observability-ops, dx-docs, dependency-supply-chain) and in **either** mode it FINDS problems and cites `file:line` evidence, never writing code. Audit mode sweeps the repo and asks "what is wrong here"; diff-scoped mode is handed a changeset and asks "does this diff introduce a regression", bounded to the changed hunks and their blast radius. The agent→domain dispatch map for audit mode lives in `orchestration.md`; the surface→reviewer map for diff-scoped mode lives in the orchestrator. Each reviewer carries a `## Diff-scoped mode` section — a sibling of its audit-mode declaration, not a replacement for it.
- **All agents return the same finding schema in BOTH modes** (title, area, severity, likelihood, evidence, why_it_matters, proposed_fix, acceptance, pr_size, labels, confidence). That is what lets one set of reviewers serve two orchestrators, so it is the invariant to protect: when editing one reviewer's output contract, keep it consistent with the schema in `orchestration.md` and the other agents, and never let a mode grow a field of its own. Blocking-vs-Nits is a *derivation* the PR orchestrator performs from `severity`, `confidence`, and a policy citation the finding's own `evidence` carries — reviewers do not pre-split, and the orchestrator never promotes on an impression the schema cannot express.
- `github-secrets` and `testflight` are standalone capability skills — no agent orchestration.

## Conventions that matter

- **`whats-on-fire`'s rules carry parity markers, and the CI gate for them is in the OTHER repo.**
The skill has two intentional homes: this plugin copy (`SKILL.md` + `references/`) and the
flattened, self-contained copy in `Sassy-Dog/sassydog-routines` that the `daily-fire-watch` cloud
routine reads by path, which exists because plugin skills cannot load in a routine session
([#175](https://github.com/Sassy-Dog/sassydog-skills/issues/175)). Nothing used to check that the
two agreed, and on 2026-08-20 that produced a false P0 in the scheduled run — **a fix applied to
this copy would not have changed the routine's behaviour at all** ([#221](https://github.com/Sassy-Dog/sassydog-skills/issues/221)).
Every load-bearing rule now carries a `<!-- rule: <id> -->` marker beside it in *both* homes, and
`sassydog-routines`' `scripts/check-skill-parity.sh` fails when the marker sets differ. **Adding a
rule here means adding the marker here, the marker there, and a line in that repo's
`parity/whats-on-fire-rules.tsv`.** Two things not to redesign: the check compares marker
*identity* and never prose, because the two homes word the same rule differently and wrap it
differently — a wording-based check reports the routine as missing rules it plainly carries; and it
cannot move into this repo, because this repo is PUBLIC and that one is INTERNAL, so its CI can
clone this one anonymously while this one's CI cannot authenticate to it at all (the
`private`-visibility trap of [#178](https://github.com/Sassy-Dog/sassydog-skills/issues/178)). A
change spanning both repos lands **here first**, or that repo's CI clones a `main` without the new
marker and fails.
- **Skill `description` is a trigger spec, not a summary.** It is dense with quoted user phrases ("set a GitHub secret", "check TestFlight feedback") because matching those phrases is what activates the skill. When adding/editing a skill, write the description as the list of utterances that should trigger it.
- **Progressive disclosure.** SKILL.md stays thin and actionable; depth goes in `references/*.md` that the skill says to read "when you reach that phase." Don't inline reference-doc detail into SKILL.md.
- **Reviewer agents carry a "Sassy Dog calibration" section** applied only when the relevant stack is present (e.g. Doppler is the secrets source of truth, all-secrets/zero-`vars.*`, managed identity + Key Vault `kv-sassydog`, pin Actions to SHAs). New org-wide policies belong in the matching reviewer's calibration block.
- **No bare `$1`–`$9`/`$@`/`$*` in SKILL.md bodies or dev-workflow templates.** Skill bodies are arg-substitution surfaces: when a skill is invoked with args (as every thin generated skill invokes its capability skill), positional tokens in the rendered body get replaced with args tokens, corrupting embedded commands. CI greps for them (`skills/*/SKILL.md`, `.claude/skills/*/SKILL.md`, `setup-config/references/templates/*`). The guard now asserts that template pathspec is non-empty first — an empty pathspec used to make it pass while covering nothing. Use `cut -f1`/`%(format)` idioms instead, or move the snippet to a `references/` doc or bundled script — neither is substituted.
- **Outward-facing actions require preview-then-confirm.** `assess-it` never files GitHub Issues silently — it prints the full Epic + child-issue preview and files only on approval. Preserve this guard in any change.
- **Irreversible actions are gated structurally, not by ordering.** `scripts/align-labels.sh --migrate` folds a repo's one-off labels onto the canonical set, and deleting a label strips it from every issue carrying it unrecoverably. "Relabel first, delete second" is therefore control flow, not a comment: `gh label delete` has exactly ONE call site, inside `migrate_delete_gate()`, whose own body re-queries GitHub immediately above it and returns without deleting on every path that is not "zero issues still carry the old label" — including "the re-query itself failed", because unknown is not verified. The relabel loop's counters are not a parameter of that function; the migration's own success message is never the evidence. `scripts/test-label-migrate.sh` asserts the single-call-site invariant against the script's source and mutation-proves the gate. Any new destructive capability goes behind the same shape: one call site, its own fresh verification in the same body.
- **A taxonomy consumer runs the owner or reads its `taxonomy` emitter — it never transcribes the table.** Two tables exist: `scripts/align-labels.sh` (10 engineering dimensions + 4 severities) and `skills/github-issues/scripts/issue-claim.sh` (the 4 dev-workflow state labels). Each exposes `taxonomy` (`name|color|description`; no gh, jq, repo or network), and each reads the other through it — that is how `--collisions` scores both sets without a copy. `assess-it` was the counter-example: a third transcription in a reference doc, frozen at the pre-#158 colours, so #158 recolouring `tech-debt` created an `infra`/`tech-debt` collision *inside a file #158 never saw*, shipped into every repo it audited — and its `gh label create … || true` could not have corrected one anyway (issue #167). It now invokes `align-labels.sh --repo <target>`; gate 8 fails on any taxonomy colour found outside its home. Note **`${CLAUDE_PLUGIN_ROOT}` is substituted into `SKILL.md` at load time only** — not into `references/*.md` (read raw) and not into the shell environment — so a plugin script path that a reference doc needs must be resolved in `SKILL.md` and passed in.
- Conventional commits (`feat:`, `fix:`, `chore:`, `docs:`). Recent history uses PRs to `main`.

## Local development

```bash
# Run Claude Code with this plugin loaded from the working tree
claude --plugin-dir ~/Repos/sassy-dog/sassydog-skills

# Install from the published marketplace
claude plugin marketplace add Sassy-Dog/sassydog-skills
claude plugin install sassy-dog
```

After editing a skill or agent, reload via `--plugin-dir` and invoke the skill to confirm frontmatter parses and triggers fire.

## Releasing

The plugin version is **monthly-rolling CalVer** (`YYYY.M.<commits-this-month>`, e.g. `2026.7.16`) per the org Versioning spec; the committed `version` in `.claude-plugin/plugin.json` is the version-of-record. **Never hand-edit it** — stamp it:

```bash
bash scripts/stamp-version.sh   # resolves CalVer and writes .claude-plugin/plugin.json
```

Commit the stamped manifest in the release PR. Build number: N/A for this repo; tags optional. Full instance doc — including the **one-way ratchet** (no `0.x`/`1.x` may ever follow CalVer): [`docs/VERSIONING.md`](docs/VERSIONING.md). Keep `README.md`'s plugin/skill table and the agent list in sync when skills or reviewer agents are added or removed.
